openai>=1.0.0               # OpenAI client (relaxed version)
groq>=0.4.0  
langchain>=0.3.0             # Updated to support NumPy 2.x
langchain-community>=0.3.0   # Updated to support NumPy 2.x
langchain-openai>=0.2.0      # Updated to support NumPy 2.x
langsmith>=0.1.17            # Compatible with newer langchain
numpy>=2.0.0      

langgraph>=0.2.0
# Removed hard pinning of langchain-core version to resolve dependency with langgraph
# Let pip resolve langchain-core dependency within constraints

fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
sentence-transformers>=5.0.0
# llama-cpp-python==0.2.57    # uncomment for local Phi-2

httpx==0.27.0
# duckduckgo-search==5.3.0    # uncomment for DDG fallback
# tavily-python==0.3.1        # uncomment for Tavily

# ── PDF preprocessing (preprocess.py) ────────────────────────────

# pymupdf==1.24.1
# Fix for metadata-generation-failed: upgrade to a version with pre-built wheels or add build dependencies as needed.
pymupdf>=1.24.3

Pillow>=10.3.0
tqdm==4.66.4
# Relax pydantic version to allow pip to pick a compatible version for all dependencies
pydantic>=2.7.4, <3.0.0
pinecone>=2.3.0
